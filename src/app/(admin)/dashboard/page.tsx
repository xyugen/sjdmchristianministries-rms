import { type Metadata } from 'next';
import React from 'react'

export const metadata: Metadata = {
    title: "Dashboard",
  };
  
const Page = () => {
  return (
    <div>Page</div>
  )
}

export default Page